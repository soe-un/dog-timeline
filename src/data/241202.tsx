import { LABELS, TLABEL, TLINES } from "@/types/type";

const data: TLINES[] = [
  {
    type: LABELS.GREEN as TLABEL,
    time: "-",
    people: "군",
    value: "전쟁 발발로 환자 수가 폭증하는 상황에 대비하는 훈련 실시",
  },
];

export default data;
